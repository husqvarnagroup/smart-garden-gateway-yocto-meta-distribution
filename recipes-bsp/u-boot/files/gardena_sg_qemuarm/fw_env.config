# Configuration file for fw_(printenv/setenv) utility.
# There is neither flash nor U-Boot under QEMU, so the environment is kept in a
# regular file which is installed by the dummy-u-boot-env package.

# Device name		Device offset	Env. size
/etc/uboot.env		0x0000		0x10000
